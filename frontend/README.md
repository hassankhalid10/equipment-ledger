# frontend

The Next.js UI. Everything about running it and the design lives in the
[root README](../README.md) and [PLAN.md](../PLAN.md).

```bash
cp .env.example .env.local
npm install
npm run dev            # http://127.0.0.1:3000
```
