# backend

The NestJS API. Everything about running, seeding, testing and the design lives in
the [root README](../README.md) and [PLAN.md](../PLAN.md).

```bash
cp .env.example .env
npm install
npm run start:dev      # http://127.0.0.1:3001/health
```
